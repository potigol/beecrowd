n = leia_inteiro
nums = leia_inteiros(" ")
se n > 1 então
  a = nums.zip(nums.cauda).mapeie(n => n.segundo - n.primeiro)
  c = a.divida_quando(_ <> _)
  escreva c.tamanho
senão
  escreva 1
fim
