s = leia_texto
var x := 0
para c em s faça
  se c == '(' então x := x + 1
  senãose x > 0 e c == ')' então x := x - 1
  fim
fim
se x == 0 então
  escreva "Partiu RU!"
senão
  escreva "Ainda temos {x} assunto(s) pendente(s)!"
fim
