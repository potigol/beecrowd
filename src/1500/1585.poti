n = leia_inteiro
para i de 1 até n faça
  a = leia_texto
  tamanho = se a[3] == ' ' então
    a.pegue(2).inteiro * a.descarte(3).inteiro div 2
  senão
    a.pegue(3).inteiro * a.descarte(4).inteiro div 2
  fim
  escreva "{tamanho} cm2"
fim
