var n := leia_inteiro
enquanto não eof faça
  a = raiz(n div 2).inteiro
  yes = para i de 0 até a gere
    b = n - i * i
    c = raiz(b).inteiro
    c * c == b
  fim
  se yes.contém(verdadeiro) então
    escreva "YES"
  senão
    escreva "NO"
  fim
  n := leia_inteiro
fim
