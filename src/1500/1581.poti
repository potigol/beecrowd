n = leia_inteiro
para i de 1 até n faça
  k = leia_inteiro
  s = leia_textos(k)
  primeiro = s.cabeca
  iguais = s.cauda.injete(verdadeiro)((a,b) => a e b == primeiro)
  se iguais então
    escreva s.cabeca
  senão
    escreva "ingles"
  fim
fim
