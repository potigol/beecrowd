c = leia_inteiro
var l := Lista(1000, 1)
para i de 2 até 1000,
     j de i até 1000 passo i faça
  l[j] := l[j] + 1
fim
numeros = l.lista.mapeie(a => a mod 2)
para i de 1 até c faça
  n = leia_inteiro
  escreva numeros.pegue(n).injete(n)(_ - _)
fim
