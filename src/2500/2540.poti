var n := leia_inteiro
enquanto n > 0 faça
  v = leia_texto
  favoraveis = v.selecione(_ == '1').tamanho
  se favoraveis * 3 >= n * 2 então
    escreva "impeachment"
  senão
    escreva "acusacao arquivada"
  fim
  n := leia_inteiro
fim
