a = leia_inteiros(" ")
l, c, x, y = a[1], a[2], a[3], a[4]
se (x mod 2 == 0) ou (c mod 2 == 0) então
  se y mod 2 == 0 então
    escreva "Direita"
  senão
    escreva "Esquerda"
  fim
senão
  se y mod 2 == 0 então
    escreva "Esquerda"
  senão
    escreva "Direita"
  fim
fim
