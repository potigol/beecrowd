n = leia_inteiro

para i de 1 ate n faca
  a = leia_inteiros(" ")
  h, d, g = a[1], a[2], a[3]
  se g >= 150 e d >= 50 e h >= 200 e h <= 300   entao
    escreva "Sim"
  senao
    escreva "Nao"
  fim
fim
