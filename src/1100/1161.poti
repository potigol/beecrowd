var fat := Lista(21, BigInt(1))
para i de 1 até 20 faça
  fat[i+1] := fat[i] * i
fim

enquanto não eof faça
  s = leia_inteiros(" ")
  se não eof então
    escreva fat[s[1] + 1] + fat[s[2] + 1]
  fim
fim
