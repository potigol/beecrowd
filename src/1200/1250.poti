n = leia_inteiro
para i de 1 até n faça
  t = leia_inteiro
  tiros = leia_inteiros(" ")
  ações = leia_texto
  atingido = 
    para (a, b) em tiros.zip(ações)
      se a < 3 e b == 'S' ou
         a > 2 e b == 'J' gere 1 fim
  escreva atingido.tamanho
fim