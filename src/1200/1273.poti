var n := leia_inteiro
enquanto n > 0 faça
  s = leia_textos(n)
  tam = s.injete(0)((a, b) => se b.tamanho > a então b.tamanho senão a fim)
  para linha em s faça
    escreva linha formato "%{tam}s"
  fim
  n := leia_inteiro
  se n > 0 então escreva "" fim
fim
