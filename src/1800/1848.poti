para i de 1 até 3 faça
  var s := leia_texto
  var soma := 0
  enquanto s <> "caw caw" faça
    se s[1] == '*' então soma := soma + 4 fim
    se s[2] == '*' então soma := soma + 2 fim
    se s[3] == '*' então soma := soma + 1 fim
    s := leia_texto
  fim
  escreva soma
fim
