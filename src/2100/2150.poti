var vogais, entrada := leia_texto
enquanto não eof faça
  var linha := entrada
  var diff := linha - vogais
  enquanto diff <> linha faça
    linha := linha - vogais
    diff := linha - vogais
  fim
  escreva entrada.tamanho - linha.tamanho
  vogais, entrada := leia_texto
fim
