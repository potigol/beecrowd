var s := leia_textos(" ")
var maior := ""
enquanto s[1] <> "0" faça
  escreva s.mapeie(_.tamanho).junte("-")
  maior := s.injete(maior)((a,b) => se a.tamanho > b.tamanho então a senão b fim)
  s := leia_textos(" ")
fim
escreva ""
escreva "The biggest word: {maior}"
