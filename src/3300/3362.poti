entrada = leia_textos(" ")
peso, quantidade, f = entrada[1].real, entrada[2].inteiro, entrada[3].real
cookies = leia_reais(quantidade)
peso_final = cookies.injete(peso)(_ + _).arredonde(2)
escreva peso_final formato "%.2f"
se peso_final <= f entao
  escreva "ho ho ho"
senao
  escreva "papai noel precisa fazer exercicios"
fim
