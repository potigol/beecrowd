n, a, b = leia_inteiro, leia_texto, leia_texto
invb = b.mapeie(c => se c == '1' então '0' senão '1' fim)
se (n mod 2 == 0 e a == b) ou (n mod 2 == 1 e a == invb) então
    escreva "Deletion succeeded"
senão
    escreva "Deletion failed"
fim
