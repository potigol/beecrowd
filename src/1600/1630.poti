mdc(x, y: Inteiro): Inteiro =
  se x > y então mdc(x mod y, y)
  senãose x == 0 então y
  senão mdc(y mod x, x) fim

var a := leia_texto
enquanto não eof faça
  b = a.divida(" ").mapeie(_.inteiro)
  x, y = b[1], b[2]
  m = mdc(x, y)
  escreva 2 * (x+y) div m
  a := leia_texto
fim
