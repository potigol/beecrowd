m = leia_reais(" ")
a, b, c = m[1], m[2], m[3]
x = leia_texto[1]

d = escolha x
      caso 'A' => 2
      caso 'B' => 3
      caso  _  => 5
    fim

t = (a * 2 + b * 3 + c * 5) / d

escreva (t - 0.5) formato "%.0f"
